# Core dependencies
requests>=2.28.0
pyyaml>=6.0
markdown>=3.4.0
pygments>=2.14.0

# Chinese keyword translation (for auto cover feature)
translators>=5.8.0

# Browser automation (for Mermaid rendering fallback)
playwright>=1.40.0
